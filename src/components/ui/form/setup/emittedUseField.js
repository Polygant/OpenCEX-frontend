// import { toRefs, watch } from "vue";
export function emittedUseField() {
  // const { shouldValidate, modelValue } = toRefs(props);
  // // const {
  // //   value: inputValue,
  // //   errorMessage,
  // //   handleChange,
  // // } = useField(name, rules, { initialValue: modelValue });
  // const onInput = (event) => {
  //   handleChange(event, shouldValidate.value);
  //   emit("update:modelValue", inputValue.value);
  // };
  // watch(modelValue, (value) => {
  //   if (value && inputValue.value !== value) inputValue.value = value;
  // });
  // return {
  //   onInput,
  //   errorMessage,
  //   inputValue,
  // };
}
